name: Deploy to Yandex Cloud

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Yandex Cloud CLI
        run: |
          curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash
          echo "C:\Users\Даша/yandex-cloud/bin" >> 

      - name: Configure YC CLI
        env:
          SA_KEY: ${{ secrets.YC_SA_KEY }}
          FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: |
          echo "" > key.json
          yc config set service-account-key key.json
          yc config set folder-id ""

      - name: Login to Container Registry
        run: yc container registry configure-docker

      - name: Build and push backend
        env:
          REGISTRY_ID: ${{ secrets.YC_REGISTRY_ID }}
          SHA: ${{ github.sha }}
        run: |
          docker build -t cr.yandex/${REGISTRY_ID}/resume-backend:${SHA} ./backend
          docker push cr.yandex/${REGISTRY_ID}/resume-backend:${SHA}

      - name: Build and push frontend
        env:
          REGISTRY_ID: ${{ secrets.YC_REGISTRY_ID }}
          SHA: ${{ github.sha }}
        run: |
          docker build -t cr.yandex/${REGISTRY_ID}/resume-frontend:${SHA} ./frontend
          docker push cr.yandex/${REGISTRY_ID}/resume-frontend:${SHA}

      - name: Deploy to VM
        env:
          REGISTRY_ID: ${{ secrets.YC_REGISTRY_ID }}
          SHA: ${{ github.sha }}
        run: |
          cat > deploy.sh <<'SCRIPT'
#!/bin/bash
cd /home/ubuntu/app
git pull origin main
docker pull cr.yandex/${REGISTRY_ID}/resume-backend:${SHA}
docker pull cr.yandex/${REGISTRY_ID}/resume-frontend:${SHA}
docker compose up -d
SCRIPT
          yc compute ssh --name resume-app-vm --ssh-key key.json -- bash -s < deploy.sh